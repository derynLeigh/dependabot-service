# .gitguardian.yaml
version: 2

# Ignore test resources
paths-ignore:
  - "src/test/resources/**"
  - "**/*Test.java"
  - "**/test/**/*.pem"

# Explicit matches to ignore
matches-ignore:
  - name: Test RSA keys in test resources
    match: "src/test/resources/test-github-key.pem"