# GitHub Configuration Management

The application should load and validate GitHub App credentials from configuration.

## Application should load GitHub configuration from properties
* Start the application with valid GitHub configuration
* GitHub App ID should be loaded correctly
* GitHub Installation ID should be loaded correctly
* GitHub Owner should be loaded correctly
* GitHub Repositories list should be loaded correctly

## Application should provide default values for optional configuration
* Start the application with only required GitHub configuration
* Default GitHub Owner should be "test-owner"
* Default repositories list should contain default repositories